import math
print(math.ceil(3.8)) #rounds a number UP to the nearest integer, if necessary, and returns the result
print(math.sqrt(25)) #returms the square
print(math.floor(8.3)) #rounds a number DOWN to the nearest integer, if necessary, and returns the result
print(math.pow(2, 3))
print(math.pi) #returns the constant value of the pi
print(math.e) #returns the constant value of e
